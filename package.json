{
  "name": "viberadar",
  "version": "0.3.57",
  "description": "Live module map with test coverage for vibecoding projects",
  "main": "./dist/cli.js",
  "bin": {
    "viberadar": "./viberadar.js"
  },
  "scripts": {
    "build": "tsc && node scripts/bundle-ui.js",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js",
    "typecheck": "tsc --noEmit",
    "release:check": "npm run typecheck && npm run build",
    "release:patch": "npm run release:check && npm version patch && npm publish",
    "release:minor": "npm run release:check && npm version minor && npm publish",
    "release:major": "npm run release:check && npm version major && npm publish"
  },
  "type": "commonjs",
  "keywords": [
    "vibe",
    "coverage",
    "modules",
    "cli",
    "dashboard"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "viberadar.js"
  ],
  "dependencies": {
    "open": "^10.1.0",
    "chokidar": "^3.6.0",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18"
  }
}
